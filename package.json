{
  "name": "aba-research",
  "version": "0.1.0",
  "description": "LLM-based research pipelines",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsc --watch",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "scaffold": "node --import tsx scripts/scaffold.ts",
    "validate-config": "node --import tsx src/cli/validate-config.ts"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.19.31",
    "tsx": "^4.21.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "zod": "^4.3.6"
  }
}
